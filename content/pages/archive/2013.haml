Categories: archive

%h1 2013 Articles
%ol
  - Nesta::Page.find_articles.each do |page|
    - if page.date.strftime("%Y") == "2013"
      %li
        %a{ :href=>page.abspath }=page.title
        %small
          \--
          = page.date.strftime("%D %H:%M")

